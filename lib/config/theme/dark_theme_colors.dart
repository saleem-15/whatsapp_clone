import 'package:flutter/material.dart';

class DarkThemeColors {
  //dark swatch
  static const Color primaryColor = Color(0xff181A20);
  static const Color onPrimaryColor = Color(0xff1F222A);
  static const Color accentColor = Color(0xfffafafa);

  //Appbar
  static const Color appbarColor = primaryColor;

  //SCAFFOLD
  static const Color scaffoldBackgroundColor = Color(0xff0d0d0d);
  static const Color backgroundColor = Color(0xff171d2d);

  //ICONS
  static const Color appBarIconsColor = Colors.white;
  static const Color iconColor = Colors.white;

  //BUTTON
  static const Color buttonColor = accentColor;
  static const Color buttonTextColor = Colors.black;
  static const Color buttonDisabledColor = Colors.grey;
  static const Color buttonDisabledTextColor = Colors.black;

  //TEXT
  static const Color bodyTextColor = Colors.white70;
  static const Color headlinesTextColor = Colors.white;
  static const Color captionTextColor = Colors.grey;
  static const Color hintTextColor = Color(0xff686868);

  //Card
  static const Color cardColor = onPrimaryColor;

  //container
  static const Color containerColor = Color(0xff35383F);

  // TextField
  static const Color textFieldColor = onPrimaryColor;
  static const Color cursorColor = Colors.white;
  static const Color textSelectionHandleColor = Colors.grey;
  static const Color selectedTextColor = Colors.grey;

  //chip
  static const Color chipBackground = primaryColor;
  static const Color selectedChipBackground = Color(0xff404757);
  static const Color chipTextColor = Colors.white;

  // progress bar indicator
  static const Color progressIndicatorColor = Color(0xFF40A76A);

  //Radio Button
  static const Color radioColor = accentColor;

  //divider color
  static const Color dividerColor = Color(0xff2C3A4B);
}
